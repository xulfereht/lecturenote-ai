# LectureNote AI 개선 계획 v2

## 🔴 발견된 문제점

### 문제 1: 세그먼트 분리 실패
- **증상**: 첫 번째 섹션 분석 시 다른 섹션 내용이 섞여 들어감
- **원인**: `handleDeepDive`에서 전체 트랜스크립트를 전송
- **해결**: 챕터별 타임스탬프 범위에 해당하는 세그먼트만 추출하여 전송

### 문제 2: 노트 포맷 문제
- **증상**: 개념, 사례, 원칙 등이 파편화되어 읽기 어려움
- **원인**: 구조화된 데이터 나열 중심 설계
- **해결**: **스토리텔링 내러티브 우선** + 보조 요소 하단 배치

---

## 🎯 새로운 노트 포맷 설계

### 계층 구조

```
┌─────────────────────────────────────────────────────┐
│  CHAPTER HEADER                                      │
│  - 제목, 타임스탬프, 한 줄 요약                       │
└─────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────┐
│  📖 핵심 내용 (NARRATIVE)                            │
│  - 2-4 단락의 스토리텔링 형식                        │
│  - 수강자가 이해하기 쉬운 흐름으로 설명              │
│  - 핵심 개념을 자연스럽게 풀어서 설명                │
│  - 예시와 비유를 적절히 활용                         │
└─────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────┐
│  💡 핵심 포인트 (KEY TAKEAWAYS)                      │
│  - 3-5개의 핵심 포인트를 한 줄씩 정리                │
│  - 내러티브에서 배운 내용의 요약                     │
└─────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────┐
│  📚 보조 요소 (SUPPORTING ELEMENTS)                  │
│  - 용어 정의 (필요한 경우만)                         │
│  - 사례 상세 (스토리에서 언급된 것 상세화)           │
│  - 실행 체크리스트                                   │
│  - 주의사항                                          │
└─────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────┐
│  📌 근거 (EVIDENCE)                                  │
│  - 인용구 및 타임스탬프                              │
└─────────────────────────────────────────────────────┘
```

---

## 📋 구현 계획

### Phase 1: 세그먼트 분리 수정 (필수)

#### 1.1 Initial Scan 개선
- 각 챕터에 `startTime`, `endTime` 필드 추가
- VTT 파싱 시 타임스탬프 정규화

#### 1.2 Deep Dive 세그먼트 추출
```typescript
// App.tsx에서 챕터별 세그먼트만 추출
const getChapterSegment = (lines: VTTLine[], chapter: Chapter) => {
  const startSeconds = parseTimestamp(chapter.startTime);
  const endSeconds = parseTimestamp(chapter.endTime);
  
  return lines.filter(line => {
    const lineSeconds = parseTimestamp(line.start);
    return lineSeconds >= startSeconds && lineSeconds <= endSeconds;
  });
};
```

### Phase 2: 새로운 스키마 설계

#### 2.1 Deep Dive 출력 스키마 (간소화)
```javascript
const CHAPTER_DEEP_DIVE_SCHEMA = {
  type: Type.OBJECT,
  properties: {
    // 1. 핵심: 스토리텔링 내러티브
    narrative: {
      type: Type.STRING,
      description: "2-4 단락의 스토리텔링 형식 설명. 수강자가 이 글만 읽어도 핵심 내용을 이해할 수 있어야 함."
    },
    
    // 2. 핵심 포인트 (3-5개)
    keyTakeaways: {
      type: Type.ARRAY,
      items: { type: Type.STRING },
      description: "핵심 포인트 3-5개. 각각 한 문장."
    },
    
    // 3. 한 줄 요약
    oneLiner: {
      type: Type.STRING,
      description: "이 챕터를 한 문장으로"
    },
    
    // 4. 보조 요소들 (선택적)
    concepts: { ... },      // 핵심 개념 (필요 시)
    examples: { ... },      // 사례 (필요 시)
    actionItems: { ... },   // 실행 항목 (필요 시)
    warnings: { ... },      // 주의사항 (필요 시)
    quotes: { ... },        // 인용구 (필요 시)
  }
}
```

### Phase 3: 프롬프트 재설계

```
당신은 학습 콘텐츠 전문 작가입니다. 다음 강의 세그먼트를 분석하여 **수강자가 쉽게 이해할 수 있는 강의노트**를 작성하세요.

## 작성 원칙
1. **스토리텔링 우선**: 딱딱한 정의 나열이 아닌, 흐름 있는 설명
2. **왜-무엇-어떻게 구조**: 왜 중요한지 → 무엇인지 → 어떻게 적용하는지
3. **비유와 예시 활용**: 추상적 개념은 구체적 사례로 설명
4. **핵심에 집중**: 이 세그먼트에서 가장 중요한 1-3가지에 집중

## 출력 구조
1. **narrative** (필수): 2-4 단락의 설명. 마치 친절한 선생님이 설명하듯.
2. **keyTakeaways** (필수): 3-5개의 핵심 포인트
3. **oneLiner** (필수): 한 문장 요약
4. 보조 요소: 필요한 것만 포함

## 중요: 이 세그먼트의 내용만 분석하세요
```

### Phase 4: UI 재설계

```
┌────────────────────────────────────────────────────────┐
│  Chapter 1: [제목]                          [00:00-12:30] │
│  💬 "한 줄 요약이 여기에"                                │
├────────────────────────────────────────────────────────┤
│                                                          │
│  📖 핵심 내용                                            │
│  ─────────────────────────────────────────────────────  │
│                                                          │
│  [스토리텔링 내러티브가 여기에. 단락 형식으로             │
│   자연스럽게 흐르는 설명. 마치 책을 읽는 것처럼           │
│   이해하기 쉬운 문체로 작성됨.]                           │
│                                                          │
│  [두 번째 단락. 개념을 더 깊이 설명하거나                 │
│   실제 적용 방법을 다룸.]                                 │
│                                                          │
├────────────────────────────────────────────────────────┤
│  💡 핵심 포인트                                          │
│  ─────────────────────────────────────────────────────  │
│  ✓ 포인트 1                                              │
│  ✓ 포인트 2                                              │
│  ✓ 포인트 3                                              │
├────────────────────────────────────────────────────────┤
│  ▼ 보조 정보 (접기/펼치기)                               │
│    - 용어 정의                                           │
│    - 실행 체크리스트                                     │
│    - 주의사항                                            │
└────────────────────────────────────────────────────────┘
```

---

## 🔧 수정해야 할 파일

| 파일 | 수정 내용 | 우선순위 |
|------|----------|---------|
| `server/index.js` | 스키마 간소화 + 스토리텔링 프롬프트 | 🔴 높음 |
| `App.tsx` | 세그먼트 분리 로직 추가 | 🔴 높음 |
| `types.ts` | 새로운 타입 정의 | 🔴 높음 |
| `LecturePreview.tsx` | 스토리텔링 중심 UI | 🟠 중간 |

---

## ✅ 다음 단계

1. **세그먼트 분리 로직 구현** (가장 급함)
2. **스키마 및 프롬프트 재설계**
3. **UI 재설계**

이 계획대로 진행할까요?
